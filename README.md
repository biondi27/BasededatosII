# BasededatosII
Base de datos local en tiempo real usando JSON.

Añadir main.py y db_instance.py a un mismo directorio para que funcione el programam completo.

Al correr main.py, se crearán dos archivos tipo JSON (primary y backup), que servirán como las bases de datos, además de un directorio _pycache_ que usará el programa.
